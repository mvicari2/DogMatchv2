# ignore all static user uploaded album images (all files in directory)
/*
# but do not ignore the .gitignore file
!.gitignore

# do not ignore AlbumImages dir itself
!/AlbumImages
# but ignore files and sub dirs in the /AlbumImages
/AlbumImages/*
