@using DogMatch.Client.Services
@inject NavigationService navigate

<AuthorizeView>
    <Authorized>
        <div class="center-align">
            @if (Owner == context.User.Identity.Name)
            {
                <MatOverline>
                    Biography Profile not completed for @DogName,
                    Cannot Display Biography.
                    <br />
                    <button class="btn btn-primary"
                            type="button"
                            @onclick="() => navigate.ToBiography(DogId)">
                        Complete Biography Profile Now
                    </button>
                </MatOverline>
            }
            else
            {
                <MatOverline>
                    Owner Has Not Completed Dog Biography Profile for @DogName.
                </MatOverline>
            }
        </div>
        <br />
    </Authorized>
</AuthorizeView>

@code {
    [Parameter]
    public int DogId { get; set; }
    [Parameter]
    public string DogName { get; set; }
    [Parameter]
    public string Owner { get; set; }
}
